import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

module.exports = {
  /*
  webpackDevMiddleware: config => {
    config.watchOptions = {
      poll: 1000, // Réduit les problèmes de hot reload
      aggregateTimeout: 300,
    };
    return config;
  },
  */
};